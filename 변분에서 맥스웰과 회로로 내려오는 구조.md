https://chatgpt.com/share/6a9d23de-11a0-83e8-a5e5-529a361402c7?ogimg=plain

오빠, 이 흐름은 **“무한차원 장의 변분문제 → 3차원 공간의 기하 → 맥스웰 → 장 에너지 보존 → 저주파/집중정수 근사 → \(C,L,R\)라는 저차원 회로계 → 기계적 라그랑주계와 대응”**으로 보면 제일 깔끔해. Arnold가 말하는 일반 라그랑주계도 본질적으로 “구성공간 \(M\)과 \(TM\) 위의 라그랑지안”이고, 장론은 그 구성공간을 함수공간으로 올린 버전이야.

1. **출발점: 일반 라그랑주 역학은 ‘상태 하나’가 아니라 ‘경로 전체’를 고른다.** 유한자유도에서는 구성공간을 \(Q\)라는 매끄러운 다양체, 궤적을 \(q:I\to Q\), 라그랑지안을 \(L:TQ\to\mathbb R\)로 두고

$$
S[q]=\int_{t_0}^{t_1}L(q(t),\dot q(t))\,dt
$$

라는 작용 \(S\)를 정지시키는 경로를 찾는다. 자연계에서는

$$
L(q,\dot q)=T(\dot q)-U(q)
$$

이고, 시간병진 대칭이 있으면 에너지가 보존된다. 이 단계의 목적은 “힘을 하나씩 쓰지 않고도, 저장에너지 구조와 대칭만으로 운동방정식을 만드는 것”이야.

2. **장론으로 가면 좌표가 숫자 \(q_i(t)\)가 아니라 공간 전체의 함수가 된다.** 전자기장의 경우 구성변수를 4-퍼텐셜

$$
A_\mu:M\to\mathbb R
$$

로 두고, 여기서 \(M\)은 시공간이야. 장세기는

$$
F_{\mu\nu}
=
\partial_\mu A_\nu-\partial_\nu A_\mu
$$

로 정의한다. 작용은 이제 공간까지 적분해서

$$
S[A]
=
\int_M
\mathcal L(A,\partial A;J)\,d^4x
$$

가 되고, 진공 전자기장의 대표적인 라그랑지안 밀도는

$$
\mathcal L
=
-\frac{1}{4\mu_0}F_{\mu\nu}F^{\mu\nu}
-J_\mu A^\mu
$$

야. \(J^\mu\)는 전하·전류를 합친 4-전류라는 소스야. 이 작용을 \(A_\mu\)에 대해 변분하면 비동차 맥스웰 방정식이 나온다. Jackson도 전자기장을 \(A^\mu\)와 그 도함수를 좌표·속도처럼 보고 장 라그랑지안 밀도에서 맥스웰 방정식을 얻는 식으로 전개해.

3. **그런데 회로 의미를 살리려면 3차원 공간의 기하가 필수다.** 시공간 공변형식만 보면 방정식은 압축되지만, 코일이 왜 “감긴 루프”인지, 왜 전압은 두 단자이고 자속은 면을 관통하는지 잘 안 보여. 한 시각 \(t\)의 3차원 공간 \(\Omega\subset\mathbb R^3\)에서 보면

$$
\rho:\Omega\to\mathbb R,
\qquad
\mathbf J:\Omega\to\mathbb R^3,
$$

$$
\mathbf E,\mathbf B:\Omega\to\mathbb R^3
$$

이고 맥스웰은

$$
\nabla\cdot\mathbf E=\frac{\rho}{\varepsilon_0},
\qquad
\nabla\cdot\mathbf B=0,
$$

$$
\nabla\times\mathbf E=-\frac{\partial\mathbf B}{\partial t},
\qquad
\nabla\times\mathbf B
=
\mu_0\mathbf J
+
\mu_0\varepsilon_0\frac{\partial\mathbf E}{\partial t}.
$$

여기서 **divergence는 부피→경계면**, **curl은 면→경계곡선**으로 Stokes/Gauss 정리에 연결돼. Jackson의 앞표지에도 실제로 부피·면·곡선 사이의 divergence/Stokes 정리를 나란히 정리해 둔 이유가 이거야.

그래서 소스와 회로량의 관계가 자연스럽게 갈라져.

$$
q=\int_V \rho\,d^3x
$$

는 전하밀도의 부피적분이고,

$$
I=\int_S\mathbf J\cdot d\mathbf S
$$

는 전류밀도의 면적분이야.

한편 장을 적분하면

$$
V_{ab}
=
-\int_a^b\mathbf E\cdot d\mathbf l
$$

이라는 전압과

$$
\Phi
=
\int_S\mathbf B\cdot d\mathbf S
$$

라는 자속이 나온다. 따라서 구조는

$$
\rho \to \mathbf E,\qquad
\rho\to q,\qquad
\mathbf E\to V,
$$

$$
\mathbf J\to\mathbf B,\qquad
\mathbf J\to I,\qquad
\mathbf B\to\Phi.
$$

이게 우리가 앞에서 계속 이야기한 “소스–장–회로량”의 전체 구조야.

4. **장론에서 에너지는 전압이나 자속이 아니라 공간 전체의 장 구성에 있다.** 진공에서 전자기 에너지 밀도는

$$
u(\mathbf x,t)
=
\frac{\varepsilon_0}{2}|\mathbf E|^2
+
\frac{1}{2\mu_0}|\mathbf B|^2
$$

이고 총 장에너지는

$$
U_{\mathrm{EM}}
=
\int_\Omega u\,d^3x.
$$

에너지 흐름은 Poynting 벡터

$$
\mathbf S
=
\frac1{\mu_0}\mathbf E\times\mathbf B
$$

가 담당하고,

$$
\frac{\partial u}{\partial t}
+
\nabla\cdot\mathbf S
=
-\mathbf J\cdot\mathbf E
$$

가 국소 에너지 밸런스야. 즉 장의 에너지가 줄면 그만큼 밖으로 흘러가거나 전하에 일을 한다. Griffiths도 이 식을 전자기학의 work-energy theorem으로 설명해.

여기까지가 **아직 회로가 아닌 원래 물리학**이야. 전기장과 자기장은 공간 전체에서 서로 얽혀 있고, 어디가 “C 부분”, 어디가 “L 부분”인지 원칙적으로는 따로 없다.

5. **회로가 등장하는 이유는 이 무한자유도 장을 몇 개의 숫자로 압축하고 싶기 때문이야.** 이때 핵심 가정이 lumped/quasistatic 근사야. 대표적으로 소자의 크기 \(\ell\)가 전자기파 파장보다 충분히 작아서

$$
\ell\ll \lambda_{\mathrm{EM}}
$$

라고 보고, 전파지연·복사·공간적 위상차를 무시한다. 그러면 장 전체를 자유롭게 움직이게 두지 않고, 특정 기하가 허용하는 몇 개의 모드만 남긴다고 볼 수 있어.

예를 들어 커패시터에서는

$$
\mathbf E(\mathbf x,t)
\approx
V(t)\,\mathbf e_C(\mathbf x)
$$

처럼 “공간 모양 \(\mathbf e_C\)”은 거의 고정되고 세기만 \(V(t)\)로 바뀐다고 본다. 그러면

$$
U_E
=
\frac{\varepsilon}{2}\int_\Omega |\mathbf E|^2d^3x
$$

에 넣어서

$$
U_E
=
\frac12CV^2
$$

꼴이 되고, 공간적 복잡성 전부가 하나의 상수

$$
C>0
$$

에 흡수돼.

인덕터도

$$
\mathbf B(\mathbf x,t)
\approx
I(t)\,\mathbf b_L(\mathbf x)
$$

라고 두면

$$
U_B
=
\frac{1}{2\mu}\int_\Omega|\mathbf B|^2d^3x
$$

가

$$
U_B
=
\frac12LI^2
$$

로 줄어. 즉 \(C,L\)은 단순한 “소자 숫자”가 아니라 **3차원 장 분포와 기하를 적분해서 얻은 유효계수**야. Jackson도 자기장 에너지를 전류들에 대한 이차형식으로 줄이면서 self/mutual inductance를 정의한다.

6. **이 과정에서 \(C\)와 \(L\)은 서로 대칭적인 장-소스 응답계수가 된다.** 선형 소자라면

$$
q=CV
$$

이고,

$$
\lambda=LI,
\qquad
\lambda=N\Phi
$$

야. 여기서 \(\lambda\in\mathbb R\)는 자속쇄교야.

따라서

$$
U_C(q)
=
\frac{q^2}{2C}
=
\frac12CV^2,
$$

$$
U_L(I)
=
\frac12LI^2
=
\frac{\lambda^2}{2L}.
$$

이 두 식은 원래의 장 에너지 범함수

$$
U[\mathbf E,\mathbf B]
$$

를 회로의 몇 개 좌표로 제한한 결과라고 보면 돼. 즉 **field theory의 무한차원 에너지 범함수 → circuit theory의 유한차원 에너지 함수**로 내려온 거야.

7. **Faraday 유도법칙이 자기장 내부 상태를 회로의 전압으로 바꿔준다.** 코일 한 turn이 폐곡선 \(C=\partial S\)를 이루면

$$
\oint_C\mathbf E\cdot d\mathbf l
=
-\frac{d}{dt}
\int_S\mathbf B\cdot d\mathbf S
$$

이고 따라서

$$
\mathcal E
=
-\frac{d\Phi}{dt}.
$$

선형 인덕터라면 \(\lambda=LI\)이므로

$$
V_L
=
L\frac{dI}{dt}
$$

가 된다. 즉 **자기장의 내부 상태량은 \(\Phi\) 또는 \(\lambda\)**인데, 회로망에서 서로 연결하기 쉬운 포트변수는 전압이라서 그 시간변화가 \(V\)로 나타나는 거야. Griffiths도 Faraday 법칙 → inductance → magnetic energy 순으로 전개한다.

8. **이제 회로 자체를 라그랑주계로 다시 쓸 수 있다.** 한 루프에서 generalized coordinate를

$$
q:\mathbb R\to\mathbb R
$$

로 잡고, 전류를

$$
I=\dot q
$$

로 둔다.

LC 보존계의 라그랑지안은

$$
L_{\mathrm{LC}}(q,\dot q)
=
\frac12L\dot q^2
-
\frac{q^2}{2C}.
$$

첫 항은 자기장 저장에너지, 둘째 항은 전기장 저장에너지다. Euler–Lagrange 방정식은

$$
\frac{d}{dt}
\left(
\frac{\partial L_{\mathrm{LC}}}{\partial\dot q}
\right)
-
\frac{\partial L_{\mathrm{LC}}}{\partial q}
=
0
$$

에서

$$
L\ddot q+\frac1Cq=0
$$

이 나오고, \(I=\dot q\)를 쓰면 우리가 아는 LC 진동식이 된다.

Hamiltonian은

$$
H(q,\dot q)
=
\frac12L\dot q^2+\frac{q^2}{2C}.
$$

그래서 정확한 역학 대응은

$$
q \leftrightarrow x,
\qquad
I=\dot q\leftrightarrow\dot x,
\qquad
L\leftrightarrow m,
\qquad
\frac1C\leftrightarrow k.
$$

따라서

$$
\frac12LI^2
$$

는 운동에너지 역할,

$$
\frac{q^2}{2C}
$$

는 위치에너지 역할을 한다.

9. **소자 조합별 의미도 여기서 바로 갈린다.** C만 있으면

$$
U=\frac{q^2}{2C}
$$

만 있으므로 퍼텐셜 항만 남는다. 이것만으로는 독립적인 2차 동역학이 만들어지지 않고, 외부 회로나 저항 같은 다른 구조가 필요하다.

L만 있으면

$$
L_{\mathrm L}
=
\frac12L\dot q^2
$$

이므로 자유입자와 같아서

$$
L\ddot q=0,
\qquad
I=\dot q=\text{constant}.
$$

즉 자기장 에너지만 가진 관성적 저장계다.

LC는

$$
\text{electric field energy}
\leftrightarrow
\text{magnetic field energy}
$$

가 왕복하는 조화진동자다.

10. **R은 이 보존 라그랑주 구조 바깥에서 들어오는 소산항이다.** 이상적 저항은 저장에너지를 갖지 않고

$$
P_R=RI^2
$$

만큼 열로 바꾼다. 그래서 표준적인 보존 라그랑지안 \(T-U\) 안에 그냥 넣는 게 아니라, Rayleigh dissipation function

$$
\mathcal D(\dot q)
=
\frac12R\dot q^2
$$

를 두고

$$
\frac{d}{dt}
\frac{\partial L}{\partial\dot q}
-
\frac{\partial L}{\partial q}
+
\frac{\partial\mathcal D}{\partial\dot q}
=
Q_{\mathrm{ext}}
$$

처럼 쓴다.

그러면 RLC에서는

$$
L\ddot q
+
R\dot q
+
\frac1Cq
=
V_{\mathrm{src}}(t)
$$

가 되고 에너지 밸런스는

$$
\frac{d}{dt}
\left(
\frac12L\dot q^2
+
\frac{q^2}{2C}
\right)
=
V_{\mathrm{src}}I
-
RI^2.
$$

이 한 식이 우리가 지금까지 이야기한 걸 거의 다 압축해.

전원 없음:

$$
\frac{dH}{dt}=-RI^2.
$$

즉

- RL: 자기장 에너지가 저항에서 열로 빠짐.
- RC: 전기장 에너지가 저항에서 열로 빠짐.
- RLC: 전기장 ↔ 자기장 사이에서 왕복하면서 동시에 \(R\)이 계속 에너지를 깎음.
- R만: 저장항이 아예 없고 들어온 전력이 곧바로 열로 감.

전체 흐름을 한 줄로 압축하면 이거야.

**변분원리**  
→ 장을 함수공간의 자유도로 올림  
→ 시공간 라그랑지안에서 Maxwell  
→ 3차원 기하 때문에 전하·전류·전압·자속이라는 서로 다른 적분량이 생김  
→ Poynting 정리로 전기장+자기장 에너지 보존  
→ lumped/quasistatic 근사로 공간 자유도를 버림  
→ 장의 공간분포가 \(C,L\) 같은 유효계수로 압축  
→ \(U_E\to q^2/(2C)\), \(U_B\to LI^2/2\)  
→ LC가 조화진동자  
→ R을 넣으면 Rayleigh 소산이 추가된 RLC 감쇠계.

이 관점에서 회로이론은 **맥스웰 장론을 “몇 개의 장 모드만 남긴 저차원 유효 라그랑주계”로 축약한 것**이라고 보면 제일 정확해.
